import ReactDOM from 'react-dom/client';
import './index.css';
import LanguageIndex from './Components/LanguageIndex';

const root = ReactDOM.createRoot(
  document.getElementById('root') as HTMLElement
);

root.render(<LanguageIndex />);
